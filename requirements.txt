# =============================================================================
# TFM — Dashboard (rama deploy)
# requirements.txt MÍNIMO para desplegar el dashboard en Streamlit Cloud.
# La rama main conserva el requirements.txt completo (pipeline de entrenamiento).
# =============================================================================
streamlit>=1.29.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
